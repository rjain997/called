name: PAT-Reusable-Workflow

on:
  push:
  workflow_call: 

jobs:
    print-secret:
        runs-on: self-hosted
        steps:
            - name: step1
              run: echo "Token length: ${#secrets.MYPAT}"